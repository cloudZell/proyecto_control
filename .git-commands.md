# Comandos Git para Subir el Proyecto

Sigue estos comandos en orden para subir tu proyecto a GitHub de forma segura:

## ⚠️ ANTES DE COMENZAR

Asegúrate de que:
- ✅ El archivo `.gitignore` esté actualizado
- ✅ No hay credenciales en el código
- ✅ El archivo `serviceAccountKey.json.json` NO está en el repositorio

## 📝 Comandos

```bash
# 1. Inicializar el repositorio (si no está inicializado)
git init

# 2. Agregar todos los archivos (excepto los que están en .gitignore)
git add .

# 3. Verificar qué archivos se van a subir (revisa que NO estén los archivos sensibles)
git status

# 4. Hacer el primer commit
git commit -m "Initial commit: Sistema de control de asistencia con QR"

# 5. Cambiar a la rama main (si es necesario)
git branch -M main

# 6. Agregar el repositorio remoto
git remote add origin https://github.com/cloudZell/proyecto_control.git

# 7. Subir el código a GitHub
git push -u origin main
```

## 🔄 Si ya tienes un repositorio

Si el repositorio ya existe y quieres actualizarlo:

```bash
# Verificar el estado
git status

# Agregar cambios
git add .

# Hacer commit
git commit -m "Descripción de los cambios"

# Subir cambios
git push origin main
```

## ✅ Verificación

Después de subir, verifica en GitHub que:
- ✅ No aparece `serviceAccountKey.json.json`
- ✅ No aparece `.env`
- ✅ Los archivos de ejemplo (`.example`) están presentes
- ✅ El README.md está actualizado

## 🚨 Si accidentalmente subiste credenciales

Si subiste credenciales por error, debes:

1. **Eliminar el archivo del historial de Git**:
```bash
git rm --cached serviceAccountKey.json.json
git commit -m "Remove sensitive file"
git push origin main
```

2. **Rotar las credenciales en Firebase**:
   - Ve a Firebase Console
   - Genera nuevas credenciales
   - Elimina las antiguas

3. **Actualizar el .gitignore** para asegurarte de que no vuelva a pasar

